openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Flow POS SaaS API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: auth
    description: Authentication
  - name: admin
    description: Super admin operations
  - name: tenant
    description: Tenant management
  - name: products
    description: Product management
  - name: categories
    description: Category management
  - name: orders
    description: Order management
  - name: customers
    description: Customer management
  - name: employees
    description: Employee management
  - name: inventory
    description: Inventory management
  - name: reports
    description: Reports and analytics
  - name: subscriptions
    description: Subscription plans

paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  # ── Auth ──────────────────────────────────────────────────────────────────
  /auth/login:
    post:
      operationId: login
      tags: [auth]
      summary: Login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginInput"
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          description: Invalid credentials

  /auth/register:
    post:
      operationId: register
      tags: [auth]
      summary: Register new tenant
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterInput"
      responses:
        "201":
          description: Registration successful
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"

  /auth/me:
    get:
      operationId: getMe
      tags: [auth]
      summary: Get current user
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"

  /auth/logout:
    post:
      operationId: logout
      tags: [auth]
      summary: Logout
      responses:
        "200":
          description: Logged out

  # ── Super Admin ─────────────────────────────────────────────────────────
  /admin/stats:
    get:
      operationId: getAdminStats
      tags: [admin]
      summary: Get super admin dashboard statistics
      responses:
        "200":
          description: Admin stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminStats"

  /admin/tenants:
    get:
      operationId: listAdminTenants
      tags: [admin]
      summary: List all tenants
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: status
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Tenant list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TenantListResponse"

  /admin/tenants/{id}:
    get:
      operationId: getAdminTenant
      tags: [admin]
      summary: Get tenant details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"
    delete:
      operationId: deleteAdminTenant
      tags: [admin]
      summary: Delete tenant
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  /admin/tenants/{id}/status:
    patch:
      operationId: updateTenantStatus
      tags: [admin]
      summary: Suspend or activate tenant
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TenantStatusUpdate"
      responses:
        "200":
          description: Updated tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"

  # ── Tenant ──────────────────────────────────────────────────────────────
  /tenant:
    get:
      operationId: getTenant
      tags: [tenant]
      summary: Get current tenant
      responses:
        "200":
          description: Tenant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"
    patch:
      operationId: updateTenant
      tags: [tenant]
      summary: Update current tenant settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TenantUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Tenant"

  /tenant/subscription:
    get:
      operationId: getTenantSubscription
      tags: [tenant]
      summary: Get current subscription
      responses:
        "200":
          description: Subscription
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Subscription"

  # ── Subscription Plans ───────────────────────────────────────────────────
  /subscriptions/plans:
    get:
      operationId: listSubscriptionPlans
      tags: [subscriptions]
      summary: List available subscription plans
      responses:
        "200":
          description: Plans
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SubscriptionPlan"

  # ── Categories ───────────────────────────────────────────────────────────
  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List categories
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"
    post:
      operationId: createCategory
      tags: [categories]
      summary: Create category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"

  /categories/{id}:
    patch:
      operationId: updateCategory
      tags: [categories]
      summary: Update category
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
    delete:
      operationId: deleteCategory
      tags: [categories]
      summary: Delete category
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── Products ─────────────────────────────────────────────────────────────
  /products:
    get:
      operationId: listProducts
      tags: [products]
      summary: List products
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: categoryId
          in: query
          schema:
            type: integer
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Products
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductListResponse"
    post:
      operationId: createProduct
      tags: [products]
      summary: Create product
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"

  /products/top:
    get:
      operationId: getTopProducts
      tags: [products]
      summary: Get top selling products
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
        - name: branchId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Top products
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TopProduct"

  /products/{id}:
    get:
      operationId: getProduct
      tags: [products]
      summary: Get product
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Product
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
    patch:
      operationId: updateProduct
      tags: [products]
      summary: Update product
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
    delete:
      operationId: deleteProduct
      tags: [products]
      summary: Delete product
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── Orders ───────────────────────────────────────────────────────────────
  /orders:
    get:
      operationId: listOrders
      tags: [orders]
      summary: List orders
      parameters:
        - name: status
          in: query
          schema:
            type: string
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Orders
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderListResponse"
    post:
      operationId: createOrder
      tags: [orders]
      summary: Create order (POS sale)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"

  /orders/recent:
    get:
      operationId: getRecentOrders
      tags: [orders]
      summary: Get recent orders for activity feed
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
        - name: branchId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Recent orders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Order"

  /orders/{id}:
    get:
      operationId: getOrder
      tags: [orders]
      summary: Get order details
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"

  /orders/{id}/status:
    patch:
      operationId: updateOrderStatus
      tags: [orders]
      summary: Update order status
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/OrderStatusUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Order"

  # ── Customers ────────────────────────────────────────────────────────────
  /customers:
    get:
      operationId: listCustomers
      tags: [customers]
      summary: List customers
      parameters:
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Customers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerListResponse"
    post:
      operationId: createCustomer
      tags: [customers]
      summary: Create customer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"

  /customers/{id}:
    get:
      operationId: getCustomer
      tags: [customers]
      summary: Get customer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Customer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
    patch:
      operationId: updateCustomer
      tags: [customers]
      summary: Update customer
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"

  # ── Employees ────────────────────────────────────────────────────────────
  /employees:
    get:
      operationId: listEmployees
      tags: [employees]
      summary: List employees
      parameters:
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Employees
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Employee"
    post:
      operationId: createEmployee
      tags: [employees]
      summary: Create employee
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"

  /employees/{id}:
    patch:
      operationId: updateEmployee
      tags: [employees]
      summary: Update employee
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmployeeUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Employee"
    delete:
      operationId: deleteEmployee
      tags: [employees]
      summary: Delete employee
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── Inventory ────────────────────────────────────────────────────────────
  /inventory:
    get:
      operationId: listInventory
      tags: [inventory]
      summary: List inventory items with stock levels
      parameters:
        - name: lowStock
          in: query
          schema:
            type: boolean
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Inventory
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryItem"

  /inventory/adjustments:
    post:
      operationId: createInventoryAdjustment
      tags: [inventory]
      summary: Create stock adjustment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InventoryAdjustmentInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InventoryAdjustment"

  /inventory/adjustments/list:
    get:
      operationId: listInventoryAdjustments
      tags: [inventory]
      summary: List inventory adjustments
      parameters:
        - name: productId
          in: query
          schema:
            type: integer
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Adjustments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/InventoryAdjustment"

  # ── Reports ──────────────────────────────────────────────────────────────
  /reports/dashboard:
    get:
      operationId: getDashboardStats
      tags: [reports]
      summary: Get tenant dashboard summary stats
      parameters:
        - name: branchId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

  /reports/sales:
    get:
      operationId: getSalesReport
      tags: [reports]
      summary: Get sales report
      parameters:
        - name: period
          in: query
          schema:
            type: string
            enum: [today, week, month, year]
        - name: dateFrom
          in: query
          schema:
            type: string
        - name: dateTo
          in: query
          schema:
            type: string
        - name: branchId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Sales report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesReport"

  /reports/sales/chart:
    get:
      operationId: getSalesChartData
      tags: [reports]
      summary: Get sales chart data over time
      parameters:
        - name: period
          in: query
          schema:
            type: string
            enum: [week, month, year]
        - name: branchId
          in: query
          schema:
            type: integer
      responses:
        "200":
          description: Chart data
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SalesChartPoint"


  # ── Branches ─────────────────────────────────────────────────────────────
  /branches:
    get:
      operationId: listBranches
      tags: [tenant]
      summary: List all branches
      responses:
        "200":
          description: Branches
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Branch"
    post:
      operationId: createBranch
      tags: [tenant]
      summary: Create branch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BranchInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Branch"

  /branches/{id}:
    patch:
      operationId: updateBranch
      tags: [tenant]
      summary: Update branch
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BranchUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Branch"
    delete:
      operationId: deleteBranch
      tags: [tenant]
      summary: Delete branch
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── Custom Roles ─────────────────────────────────────────────────────────
  /roles:
    get:
      operationId: listRoles
      tags: [tenant]
      summary: List all custom roles
      responses:
        "200":
          description: Custom roles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CustomRole"
    post:
      operationId: createRole
      tags: [tenant]
      summary: Create custom role
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomRoleInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomRole"

  /roles/{id}:
    patch:
      operationId: updateRole
      tags: [tenant]
      summary: Update custom role
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomRoleInput"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomRole"
    delete:
      operationId: deleteRole
      tags: [tenant]
      summary: Delete custom role
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted

  # ── Announcements ────────────────────────────────────────────────────────
  /announcements:
    get:
      operationId: listAnnouncements
      tags: [tenant]
      summary: List all announcements
      responses:
        "200":
          description: Announcements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Announcement"

  /admin/announcements:
    post:
      operationId: createAnnouncement
      tags: [admin]
      summary: Create announcement (admin only)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnnouncementInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Announcement"

  # ── Support Tickets ──────────────────────────────────────────────────────
  /support/tickets:
    get:
      operationId: listSupportTickets
      tags: [tenant]
      summary: List support tickets
      responses:
        "200":
          description: Tickets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SupportTicket"
    post:
      operationId: createSupportTicket
      tags: [tenant]
      summary: Create support ticket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SupportTicketInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SupportTicket"

  /support/tickets/{id}/replies:
    get:
      operationId: listTicketReplies
      tags: [tenant]
      summary: List replies for ticket
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Replies
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/TicketReply"
    post:
      operationId: createTicketReply
      tags: [tenant]
      summary: Reply to ticket
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TicketReplyInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketReply"

  # ── Admin Config & Impersonation ──────────────────────────────────────────
  /admin/tenants/{id}/impersonate:
    post:
      operationId: impersonateTenant
      tags: [admin]
      summary: Impersonate a tenant's owner
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Token response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"

  /admin/settings:
    get:
      operationId: getAdminSettings
      tags: [admin]
      summary: Get platform global settings
      responses:
        "200":
          description: Platform settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformSettings"
    patch:
      operationId: updateAdminSettings
      tags: [admin]
      summary: Update platform global settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlatformSettingsInput"
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformSettings"

  /admin/security-logs:
    get:
      operationId: getSecurityLogs
      tags: [admin]
      summary: Get security audit logs
      responses:
        "200":
          description: Security logs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SecurityLog"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]

    # ── Auth ──────────────────────────────────────────────────────────────
    LoginInput:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
        password:
          type: string

    RegisterInput:
      type: object
      required: [name, email, password, businessName, businessType]
      properties:
        name:
          type: string
        email:
          type: string
        password:
          type: string
        businessName:
          type: string
        businessType:
          type: string
          enum: [fnb, fashion, restaurant, cafe, salon, minimarket]
        phone:
          type: ["string", "null"]
        address:
          type: ["string", "null"]
        plan:
          type: string
          enum: [starter, business, pro, custom, trial]


    AuthResponse:
      type: object
      required: [token, user]
      properties:
        token:
          type: string
        user:
          $ref: "#/components/schemas/User"

    User:
      type: object
      required: [id, name, email, role, tenantId]
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
        role:
          type: string
          enum: [super_admin, owner, manager, cashier, kitchen_staff, staff, delivery_staff]
        tenantId:
          type: ["integer", "null"]
        createdAt:
          type: string

    # ── Admin ─────────────────────────────────────────────────────────────
    AdminStats:
      type: object
      required: [totalTenants, activeTenants, suspendedTenants, trialUsers, expiredSubscriptions, monthlyRevenue, annualRevenue, totalOrders, totalCustomers, totalEmployees, totalBranches, totalTransactions, totalUsers]
      properties:
        totalTenants:
          type: integer
        activeTenants:
          type: integer
        suspendedTenants:
          type: integer
        trialUsers:
          type: integer
        expiredSubscriptions:
          type: integer
        monthlyRevenue:
          type: number
        annualRevenue:
          type: number
        totalOrders:
          type: integer
        totalCustomers:
          type: integer
        totalEmployees:
          type: integer
        totalBranches:
          type: integer
        totalTransactions:
          type: integer
        totalUsers:
          type: integer
        byBusinessType:
          type: array
          items:
            $ref: "#/components/schemas/BusinessTypeCount"

    BusinessTypeCount:
      type: object
      required: [type, count]
      properties:
        type:
          type: string
        count:
          type: integer

    TenantListResponse:
      type: object
      required: [data, total, page, limit]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Tenant"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer

    TenantStatusUpdate:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [active, suspended]

    # ── Tenant ────────────────────────────────────────────────────────────
    Tenant:
      type: object
      required: [id, name, businessType, status, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        slug:
          type: ["string", "null"]
        businessType:
          type: string
          enum: [fnb, fashion, restaurant, cafe, salon, minimarket]
        status:
          type: string
          enum: [active, suspended, trial, expired]
        address:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        email:
          type: ["string", "null"]
        logoUrl:
          type: ["string", "null"]
        primaryColor:
          type: ["string", "null"]
        coverUrl:
          type: ["string", "null"]
        bio:
          type: ["string", "null"]
        subscriptionPlan:
          type: ["string", "null"]
        subscriptionExpiresAt:
          type: ["string", "null"]
        enableDelivery:
          type: boolean
        deliveryFeeNear:
          type: integer
        deliveryFeeFar:
          type: integer
        showVariants:
          type: boolean
        showToppings:
          type: boolean
        createdAt:
          type: string

    TenantUpdate:
      type: object
      properties:
        name:
          type: string
        address:
          type: string
        phone:
          type: string
        email:
          type: string
        primaryColor:
          type: string
        logoUrl:
          type: string
        coverUrl:
          type: string
        bio:
          type: string
        receiptFooter:
          type: string
        enableDelivery:
          type: boolean
        deliveryFeeNear:
          type: integer
        deliveryFeeFar:
          type: integer
        showVariants:
          type: boolean
        showToppings:
          type: boolean

    # ── Subscription ──────────────────────────────────────────────────────
    Subscription:
      type: object
      required: [id, plan, status, expiresAt]
      properties:
        id:
          type: integer
        plan:
          type: string
          enum: [trial, basic, pro, enterprise]
        status:
          type: string
          enum: [active, expired, cancelled]
        expiresAt:
          type: string
        startedAt:
          type: string
        price:
          type: number

    SubscriptionPlan:
      type: object
      required: [id, name, price, durationDays, maxBranches, features]
      properties:
        id:
          type: integer
        name:
          type: string
        price:
          type: number
        durationDays:
          type: integer
        maxBranches:
          type: ["integer", "null"]
        features:
          type: array
          items:
            type: string

    # ── Category ──────────────────────────────────────────────────────────
    Category:
      type: object
      required: [id, name, tenantId]
      properties:
        id:
          type: integer
        name:
          type: string
        description:
          type: ["string", "null"]
        tenantId:
          type: integer
        createdAt:
          type: string

    CategoryInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        description:
          type: string

    CategoryUpdate:
      type: object
      properties:
        name:
          type: string
        description:
          type: string

    # ── Product ───────────────────────────────────────────────────────────
    ProductListResponse:
      type: object
      required: [data, total, page, limit]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Product"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer

    Product:
      type: object
      required: [id, name, price, stock, tenantId]
      properties:
        id:
          type: integer
        name:
          type: string
        sku:
          type: ["string", "null"]
        barcode:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        price:
          type: number
        costPrice:
          type: ["number", "null"]
        stock:
          type: integer
        minStock:
          type: integer
        imageUrl:
          type: ["string", "null"]
        categoryId:
          type: ["integer", "null"]
        categoryName:
          type: ["string", "null"]
        tenantId:
          type: integer
        isActive:
          type: boolean
        isBestSeller:
          type: boolean
        variantSettings:
          type: ["string", "null"]
        createdAt:
          type: string

    ProductInput:
      type: object
      required: [name, price, stock]
      properties:
        name:
          type: string
        sku:
          type: string
        barcode:
          type: string
        description:
          type: string
        price:
          type: number
        costPrice:
          type: number
        stock:
          type: integer
        minStock:
          type: integer
        imageUrl:
          type: string
        categoryId:
          type: integer
        isBestSeller:
          type: boolean
        variantSettings:
          type: ["string", "null"]

    ProductUpdate:
      type: object
      properties:
        name:
          type: string
        sku:
          type: string
        barcode:
          type: string
        description:
          type: string
        price:
          type: number
        costPrice:
          type: number
        stock:
          type: integer
        minStock:
          type: integer
        imageUrl:
          type: string
        categoryId:
          type: ["integer", "null"]
        isActive:
          type: boolean
        isBestSeller:
          type: boolean
        variantSettings:
          type: ["string", "null"]

    TopProduct:
      type: object
      required: [productId, name, totalSold, revenue]
      properties:
        productId:
          type: integer
        name:
          type: string
        totalSold:
          type: integer
        revenue:
          type: number
        imageUrl:
          type: ["string", "null"]

    # ── Order ─────────────────────────────────────────────────────────────
    OrderListResponse:
      type: object
      required: [data, total, page, limit]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Order"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer

    Order:
      type: object
      required: [id, orderNumber, total, status, paymentMethod, tenantId, createdAt]
      properties:
        id:
          type: integer
        orderNumber:
          type: string
        subtotal:
          type: number
        discount:
          type: number
        tax:
          type: number
        total:
          type: number
        status:
          type: string
          enum: [pending, completed, cancelled, refunded]
        paymentMethod:
          type: string
          enum: [cash, qris, bank_transfer, ewallet, credit_card]
        notes:
          type: ["string", "null"]
        customerId:
          type: ["integer", "null"]
        customerName:
          type: ["string", "null"]
        employeeId:
          type: ["integer", "null"]
        employeeName:
          type: ["string", "null"]
        tenantId:
          type: integer
        branchId:
          type: ["integer", "null"]
        items:
          type: array
          items:
            $ref: "#/components/schemas/OrderItem"
        createdAt:
          type: string

    OrderItem:
      type: object
      required: [id, productId, productName, quantity, price, subtotal]
      properties:
        id:
          type: integer
        productId:
          type: integer
        productName:
          type: string
        quantity:
          type: integer
        price:
          type: number
        subtotal:
          type: number

    OrderInput:
      type: object
      required: [items, paymentMethod, total]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/OrderItemInput"
        paymentMethod:
          type: string
          enum: [cash, qris, bank_transfer, ewallet, credit_card]
        subtotal:
          type: number
        discount:
          type: number
        tax:
          type: number
        total:
          type: number
        notes:
          type: string
        customerId:
          type: integer
        employeeId:
          type: integer
        branchId:
          type: integer

    OrderItemInput:
      type: object
      required: [productId, quantity, price]
      properties:
        productId:
          type: integer
        quantity:
          type: integer
        price:
          type: number
        variantSelection:
          type: ["string", "null"]
        notes:
          type: ["string", "null"]

    OrderStatusUpdate:
      type: object
      required: [status]
      properties:
        status:
          type: string
          enum: [pending, completed, cancelled, refunded]

    # ── Customer ──────────────────────────────────────────────────────────
    CustomerListResponse:
      type: object
      required: [data, total, page, limit]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Customer"
        total:
          type: integer
        page:
          type: integer
        limit:
          type: integer

    Customer:
      type: object
      required: [id, name, tenantId, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        address:
          type: ["string", "null"]
        loyaltyPoints:
          type: integer
        totalSpent:
          type: number
        totalOrders:
          type: integer
        membershipLevel:
          type: string
          enum: [regular, silver, gold, platinum]
        notes:
          type: ["string", "null"]
        tenantId:
          type: integer
        createdAt:
          type: string

    CustomerInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        email:
          type: string
        phone:
          type: string
        address:
          type: string
        notes:
          type: string

    CustomerUpdate:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
        phone:
          type: string
        address:
          type: string
        notes:
          type: string

    # ── Employee ──────────────────────────────────────────────────────────
    Employee:
      type: object
      required: [id, name, role, tenantId, createdAt]
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        role:
          type: string
        isActive:
          type: boolean
        tenantId:
          type: integer
        branchId:
          type: ["integer", "null"]
        customRoleId:
          type: ["integer", "null"]
        createdAt:
          type: string

    EmployeeInput:
      type: object
      required: [name, role]
      properties:
        name:
          type: string
        email:
          type: string
        phone:
          type: string
        role:
          type: string
        branchId:
          type: ["integer", "null"]
        customRoleId:
          type: ["integer", "null"]

    EmployeeUpdate:
      type: object
      properties:
        name:
          type: string
        email:
          type: string
        phone:
          type: string
        role:
          type: string
        isActive:
          type: boolean
        branchId:
          type: ["integer", "null"]
        customRoleId:
          type: ["integer", "null"]

    # ── Inventory ─────────────────────────────────────────────────────────
    InventoryItem:
      type: object
      required: [productId, productName, stock, minStock, tenantId]
      properties:
        productId:
          type: integer
        productName:
          type: string
        sku:
          type: ["string", "null"]
        stock:
          type: integer
        minStock:
          type: integer
        isLowStock:
          type: boolean
        categoryName:
          type: ["string", "null"]
        tenantId:
          type: integer

    InventoryAdjustment:
      type: object
      required: [id, productId, type, quantity, tenantId, createdAt]
      properties:
        id:
          type: integer
        productId:
          type: integer
        productName:
          type: ["string", "null"]
        type:
          type: string
          enum: [stock_in, stock_out, adjustment]
        quantity:
          type: integer
        notes:
          type: ["string", "null"]
        tenantId:
          type: integer
        createdAt:
          type: string

    InventoryAdjustmentInput:
      type: object
      required: [productId, type, quantity]
      properties:
        productId:
          type: integer
        type:
          type: string
          enum: [stock_in, stock_out, adjustment]
        quantity:
          type: integer
        notes:
          type: string

    # ── Reports ───────────────────────────────────────────────────────────
    DashboardStats:
      type: object
      required: [todaySales, todayOrders, totalProducts, totalCustomers, lowStockCount, monthlyRevenue, weeklyRevenue]
      properties:
        todaySales:
          type: number
        todayOrders:
          type: integer
        totalProducts:
          type: integer
        totalCustomers:
          type: integer
        lowStockCount:
          type: integer
        monthlyRevenue:
          type: number
        weeklyRevenue:
          type: number
        revenueGrowth:
          type: number

    SalesReport:
      type: object
      required: [totalRevenue, totalOrders, totalItems, averageOrderValue]
      properties:
        totalRevenue:
          type: number
        totalOrders:
          type: integer
        totalItems:
          type: integer
        averageOrderValue:
          type: number
        topProducts:
          type: array
          items:
            $ref: "#/components/schemas/TopProduct"
        byPaymentMethod:
          type: array
          items:
            $ref: "#/components/schemas/PaymentMethodBreakdown"

    PaymentMethodBreakdown:
      type: object
      required: [method, count, total]
      properties:
        method:
          type: string
        count:
          type: integer
        total:
          type: number

    SalesChartPoint:
      type: object
      required: [label, revenue, orders]
      properties:
        label:
          type: string
        revenue:
          type: number
        orders:
          type: integer

    Branch:
      type: object
      required: [id, tenantId, name, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        name:
          type: string
        address:
          type: ["string", "null"]
        phone:
          type: ["string", "null"]
        createdAt:
          type: string

    BranchInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        address:
          type: string
        phone:
          type: string

    BranchUpdate:
      type: object
      properties:
        name:
          type: string
        address:
          type: string
        phone:
          type: string

    CustomRole:
      type: object
      required: [id, tenantId, name, permissions, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        name:
          type: string
        permissions:
          type: array
          items:
            type: string
        createdAt:
          type: string

    CustomRoleInput:
      type: object
      required: [name, permissions]
      properties:
        name:
          type: string
        permissions:
          type: array
          items:
            type: string

    Announcement:
      type: object
      required: [id, title, content, type, createdAt]
      properties:
        id:
          type: integer
        title:
          type: string
        content:
          type: string
        type:
          type: string
        createdAt:
          type: string

    AnnouncementInput:
      type: object
      required: [title, content, type]
      properties:
        title:
          type: string
        content:
          type: string
        type:
          type: string

    SupportTicket:
      type: object
      required: [id, tenantId, title, description, status, category, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: integer
        tenantName:
          type: ["string", "null"]
        title:
          type: string
        description:
          type: string
        status:
          type: string
        category:
          type: string
        createdAt:
          type: string
        updatedAt:
          type: string

    SupportTicketInput:
      type: object
      required: [title, description, category]
      properties:
        title:
          type: string
        description:
          type: string
        category:
          type: string

    TicketReply:
      type: object
      required: [id, ticketId, senderId, senderRole, senderName, message, createdAt]
      properties:
        id:
          type: integer
        ticketId:
          type: integer
        senderId:
          type: integer
        senderRole:
          type: string
        senderName:
          type: string
        message:
          type: string
        createdAt:
          type: string

    TicketReplyInput:
      type: object
      required: [message]
      properties:
        message:
          type: string

    PlatformSettings:
      type: object
      required: [key, value]
      properties:
        key:
          type: string
        value:
          type: object

    PlatformSettingsInput:
      type: object
      required: [value]
      properties:
        value:
          type: object

    SecurityLog:
      type: object
      required: [id, userId, userName, userRole, action, createdAt]
      properties:
        id:
          type: integer
        tenantId:
          type: ["integer", "null"]
        userId:
          type: integer
        userName:
          type: string
        userRole:
          type: string
          enum: [super_admin, owner, manager, cashier, kitchen_staff, staff, delivery_staff]
        action:
          type: string
        module:
          type: ["string", "null"]
        details:
          type: object
        ipAddress:
          type: ["string", "null"]
        createdAt:
          type: string
